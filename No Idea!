# Enter your code here. Read input from STDIN. Print output to STDOUT
n, m = input().split()
arr = list(map(int, input().split()))

A = set((int(item) for item in set(input().split())))
B = set((int(item) for item in set(input().split())))

happiness = sum(1 if element in A else -1 if element in B else 0 for element in arr)
print(happiness)
